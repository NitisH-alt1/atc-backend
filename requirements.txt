# ATC Live Backend — Python dependencies
# Install with:  pip install -r requirements.txt

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
python-dotenv>=1.0.0

# Optional — for authenticated OpenSky access (5s resolution, more data)
# Requires a free account at https://opensky-network.org/
# Comment out if you don't need it.
pyopensky>=2.8

# Optional — for Trino historical data queries (replay mode)
# trino>=0.328.0
